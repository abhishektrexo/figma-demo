<section class="landing-hero">
  <div class="watermark-june" aria-hidden="true">june</div>
  
  <div class="hero-container">
    <div class="hero-content">
      <div class="text-side reveal">
        <h1 class="fade-in-up">Protecting <span class="accent">Innovation</span> <br/>Powering <span class="accent">Insights</span></h1>
        <p class="hero-subtext fade-in-up delay-1">
          The Future of Intellectual Property is here. From managing complex portfolios to accelerating innovation, our AI-powered ecosystem simplifies every stage of the patent lifecycle.
        </p>
        <div class="hero-actions fade-in-up delay-2">
          <a href="/june" class="btn-primary">Book a Demo</a>
          <a href="/contact" class="btn-outline">See How it Works</a>
        </div>
      </div>
      
      <div class="visual-side reveal delay-1">
        <div class="mockup-layered">
          <img src="https://codia-f2c.s3.us-west-1.amazonaws.com/image/2026-02-16/c1cmUZnZY2.png" alt="Main Dashboard Mockup" class="layer-base" />
          <img src="https://codia-f2c.s3.us-west-1.amazonaws.com/image/2026-02-16/NcvkK9PP5E.png" alt="Overlay Element 1" class="layer-overlay overlay-1" />
          <img src="https://codia-f2c.s3.us-west-1.amazonaws.com/image/2026-02-16/MgRk99KZk3.png" alt="Overlay Element 2" class="layer-overlay overlay-2" />
          <img src="https://codia-f2c.s3.us-west-1.amazonaws.com/image/2026-02-16/fif70SBut5.png" alt="Overlay Element 3" class="layer-overlay overlay-3" />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .landing-hero {
    position: relative;
    min-height: 100vh;
    background-color: var(--color-dark);
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: 120px 0;
  }

  .hero-container {
    max-width: var(--container-max-width);
    margin: 0 auto;
    padding: var(--content-padding);
    width: 100%;
    position: relative;
    z-index: 10;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }

  .text-side {
    max-width: 600px;
  }

  h1 {
    font-family: var(--font-primary);
    font-size: clamp(48px, 6.5vw, 84px);
    font-weight: 400;
    line-height: 1.0;
    color: var(--color-white);
    margin-bottom: 24px;
    letter-spacing: -3px;
  }

  h1 .accent {
    font-family: var(--font-serif);
    font-weight: 400;
    font-style: italic;
    color: var(--color-primary-light);
    letter-spacing: -1px;
    margin: 0 4px;
  }

  .hero-subtext {
    font-size: 18px;
    color: var(--color-text-muted);
    margin-bottom: 48px;
    max-width: 520px;
    line-height: 1.6;
    font-weight: 400;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 24px;
  }

  .btn-primary {
    background-color: var(--color-primary);
    color: white;
    padding: 18px 40px;
    border-radius: 12px;
    font-weight: 700;
    font-size: 16px;
    text-transform: capitalize;
    transition: all 0.3s cubic-bezier(0.165, 0.84, 0.44, 1);
  }

  .btn-primary:hover {
    background-color: var(--color-primary-light);
    transform: translateY(-2px);
    box-shadow: 0 15px 30px rgba(0, 92, 185, 0.3);
  }

  .btn-outline {
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: var(--color-white);
    padding: 18px 40px;
    border-radius: 12px;
    font-weight: 600;
    font-size: 16px;
    transition: all 0.3s;
  }

  .btn-outline:hover {
    border-color: var(--color-primary-light);
    background-color: rgba(0, 120, 242, 0.05);
    transform: translateY(-2px);
  }

  /* Layered Mockup */
  .visual-side {
    position: relative;
    padding-left: 40px;
  }

  .mockup-layered {
    position: relative;
    width: 100%;
    transform: translateX(20px);
  }

  .layer-base {
    width: 100%;
    height: auto;
    border-radius: 12px;
    box-shadow: 0 40px 100px rgba(0,0,0,0.8);
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .layer-overlay {
    position: absolute;
    height: auto;
    border-radius: 8px;
    box-shadow: 0 30px 60px rgba(0,0,0,0.6);
    z-index: 2;
    transition: transform 0.5s ease-out;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .overlay-1 {
    width: 55%;
    bottom: -8%;
    left: -18%;
    z-index: 10;
  }

  .overlay-2 {
    width: 45%;
    top: -12%;
    right: -12%;
    z-index: 5;
  }

  .overlay-3 {
    width: 40%;
    top: 15%;
    right: -25%;
    z-index: 8;
  }

  @keyframes floating {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-15px); }
    100% { transform: translateY(0px); }
  }

  .watermark-june {
    position: absolute;
    bottom: -5%;
    left: -10%;
    font-family: var(--font-june);
    font-size: 40vw;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.035);
    pointer-events: none;
    line-height: 0.7;
    z-index: 1;
    letter-spacing: -2vw;
    text-transform: lowercase;
  }

  @media (max-width: 1200px) {
    .visual-side { padding-left: 0; }
    .overlay-3 { right: -15%; }
  }

  @media (max-width: 1024px) {
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 120px;
    }
    .text-side { margin: 0 auto; }
    .hero-actions { justify-content: center; }
    .mockup-layered { max-width: 500px; margin: 0 auto; transform: none; }
    .overlay-1 { left: -10%; }
    .overlay-2 { right: -10%; }
    .overlay-3 { right: -15%; }
    .watermark-june { font-size: 55vw; }
  }

  @media (max-width: 768px) {
    h1 { font-size: 48px; letter-spacing: -1.5px; }
    .hero-actions { flex-direction: column; width: 100%; gap: 16px; }
    .btn-primary, .btn-outline { width: 100%; }
    .watermark-june { font-size: 70vw; }
    .layer-overlay { display: none; }
  }
</style>
