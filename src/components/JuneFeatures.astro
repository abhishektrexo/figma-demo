---
const features = [
  {
    title: "Secure Encryption",
    description: "Military-grade encryption for all your patent data and documents.",
    icon: "Lock"
  },
  {
    title: "2FA Authentication",
    description: "Multi-layered security to ensure only authorized access.",
    icon: "2FA"
  },
  {
    title: "Smart Filtering",
    description: "Advanced search and filter capabilities for massive IP portfolios.",
    icon: "Filter"
  },
  {
    title: "Full Audit Logs",
    description: "Complete transparency with traceable history for every action.",
    icon: "Audit"
  }
];
---

<section class="june-section reveal" id="products">
  <div class="container">
    <div class="section-head center">
      <h2 id="june-heading">Everything on June's Desk</h2>
      <p>
        From foreign filings to renewals, June manages deadlines, jurisdictions, documents, and compliance every day.
      </p>
    </div>

    <div class="june-layout">
      <ul class="feature-list left-list">
        <li class="reveal delay-1">Managing IDS Workflows</li>
        <li class="reveal delay-2">Generating OA Shell Response</li>
        <li class="reveal delay-3">AppGen</li>
        <li class="reveal delay-1">Bibliographic Data</li>
      </ul>

      <div class="june-core reveal delay-2">
        <div class="orbital-icon">AI</div>
        <div class="pulse-ring"></div>
      </div>

      <ul class="feature-list right-list">
        <li class="reveal delay-1">Claims Formatter</li>
        <li class="reveal delay-2">Oath/Dec & ADS Downloader</li>
        <li class="reveal delay-3">Patent File Wrapper</li>
        <li class="reveal delay-1">Generating Forms</li>
      </ul>
    </div>
  </div>
</section>

<style>
  .june-section {
    background-color: var(--bg-light);
    color: var(--text-dark);
    padding: 100px 0;
  }

  .section-head {
    margin-bottom: 60px;
    text-align: center;
  }

  .section-head h2 {
    color: var(--text-dark);
    font-size: clamp(2rem, 4vw, 2.5rem);
    margin-bottom: 16px;
    font-weight: 700;
  }

  .section-head p {
    color: #4e5870;
    max-width: 68ch;
    margin: 0 auto;
    font-size: 1.1rem;
  }

  .june-layout {
    display: grid;
    gap: 40px;
    align-items: center;
    position: relative;
  }

  .feature-list {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 20px;
  }

  .feature-list li {
    background: #fff;
    border: 1px solid var(--line);
    border-radius: 16px;
    padding: 20px 24px;
    box-shadow: 0 10px 30px rgba(9, 20, 52, 0.05);
    font-weight: 600;
    color: var(--text-dark);
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
  }

  .feature-list li:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 40px rgba(9, 20, 52, 0.1);
    border-color: var(--primary);
  }

  .june-core {
    display: grid;
    place-items: center;
    position: relative;
    width: 320px;
    height: 320px;
    margin: 0 auto;
    background: radial-gradient(circle at 50% 45%, #fff 0%, #edf1f8 100%);
    border-radius: 50%;
    border: 1px solid #d4daea;
    z-index: 2;
  }

  .orbital-icon {
    width: 140px;
    height: 140px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 2.5rem;
    color: #fff;
    background: linear-gradient(160deg, #0b65e0, #2ea0ff);
    box-shadow: 0 20px 50px rgba(13, 110, 253, 0.4);
    position: relative;
    z-index: 5;
  }

  .pulse-ring {
    position: absolute;
    width: 100%;
    height: 100%;
    border-radius: 50%;
    border: 2px solid rgba(13, 110, 253, 0.2);
    animation: pulse 3s infinite;
  }

  @keyframes pulse {
    0% { transform: scale(1); opacity: 1; }
    100% { transform: scale(1.4); opacity: 0; }
  }

  @media (min-width: 992px) {
    .june-layout {
      grid-template-columns: 1fr auto 1fr;
    }
  }

  @media (max-width: 991px) {
    .june-layout {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .feature-list li {
      justify-content: center;
    }
    .june-core {
      order: -1;
      width: 280px;
      height: 280px;
    }
  }
</style>
